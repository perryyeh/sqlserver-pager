------------------------------------------------
Install:
1.执行sp_Util_Page.sql添加存储过程.
2.调用方法:
exec sp_Util_Page '字段1,字段2','表','条件(可为null)','排序(可为null)','主键(可为null)',页数,每页记录数,总记录数(如果<1则count),输出sql(调用时候用NULL均可)